name: SEssentials
version: 1.0.0
main: dev.iyanz.sessentials.SEssentialsPlugin
api-version: '1.21'
folia-supported: true
authors:
  - Yan
description: Sourby Essentials — a Folia-safe, clean-room essentials suite for SourbyCraft.
dependencies:
  server:
    Vault:
      load: BEFORE
      required: false
    PlaceholderAPI:
      load: BEFORE
      required: false
permissions:
  sessentials.*:
    description: All SEssentials permissions.
    default: op
