name: SEssentials
version: 1.0.0
main: dev.iyanz.sessentials.SEssentialsPlugin
loader: dev.iyanz.sessentials.SourbyLoader
api-version: '1.21'
folia-supported: true
authors:
  - Yan
description: Sourby Essentials — a Folia-safe, clean-room essentials suite for SourbyCraft.
dependencies:
  server:
    Vault:
      load: BEFORE
      required: false
      join-classpath: true
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true
permissions:
  sessentials.*:
    description: All SEssentials permissions.
    default: op
